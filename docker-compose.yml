version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./bio_predictions.db:/app/bio_predictions.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
